import { NavContainer, DesktopNav, MobileNav } from '.';

const Navigation = () => {
  return (
    <NavContainer>
      <DesktopNav />
      <MobileNav />
    </NavContainer>
  );
};

export default Navigation;
